# Development environment variables — loaded by Vite automatically when running `vite` (dev mode).
# Override these in your shell or .env.development.local (git-ignored) for machine-specific values.
#
# VITE_ prefix is required — Vite strips unprefixed vars from the browser bundle at build time.
VITE_SOCKET_URL=http://localhost:3000